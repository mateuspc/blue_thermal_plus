# Epson ePOS SDK

Copy Epson's `libepos2.xcframework` into this directory when you need
`PrinterTransport.epson` on iOS:

```text
ios/Frameworks/libepos2.xcframework
```

The framework is intentionally not committed with the plugin. Download it from
Epson and accept Epson's SDK license before using it.
